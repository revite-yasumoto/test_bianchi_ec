# お問い合わせ管理

本ドキュメントは**対応ステータスの区分と更新規則**、および**問い合わせのCSVエクスポート**の正本である。フォームの表示・送信は [docs/front/contact.md](../front/contact.md) が正本。

## 機能概要

- **対象画面・機能の目的:** フロントから送信された問い合わせを一覧・詳細で確認し、対応状況を記録する。
- **URL / メソッド:**

| メソッド | パス | ルート名 | 認証 |
|---|---|---|---|
| GET | `/admin/contacts` | `admin.contacts.index` | `auth:admin` |
| GET | `/admin/contacts/csv/export` | `admin.contacts.csv.export` | `auth:admin` |
| GET | `/admin/contacts/{contact}` | `admin.contacts.show` | `auth:admin` |
| PUT | `/admin/contacts/{contact}` | `admin.contacts.update` | `auth:admin` |

CSVのルートは `contacts/{contact}` として解釈されないよう、詳細ルートより**先に**定義する（[docs/admin/csv.md](csv.md) と同じ規則）。

- **アクセス権限・ミドルウェア:** 上表の通り。管理者ごとの権限区分は設けない（[docs/1_system_overview.md](../1_system_overview.md) 参照）。
- **本ドキュメントのスコープ:** 一覧（タブ・絞り込み・ページネーション）、詳細、対応状況の更新、CSVエクスポート。

## 使用テーブル

`contacts` を参照・更新し、表示のために `users`（送信者の会員情報）・`products`（対象商品）・`admins`（対応者）を参照する。定義は [docs/2_database.md](../2_database.md) が正本。

本機能で対応状況を保持する列を追加する。

| カラム | 型 | 用途 |
|---|---|---|
| `contacts.status` | `string(20)`・default `unhandled` | 対応ステータス（`App\Enums\ContactStatus`） |
| `contacts.admin_note` | `text`・nullable | 対応メモ。管理者のみが入力・閲覧する |
| `contacts.handled_admin_id` | `foreignId`・nullable・→ `admins.id` set null | 最後に対応状況を更新した管理者 |
| `contacts.handled_at` | `dateTime`・nullable | 対応済みへ変更した日時。対応所要時間の集計に使う |

送信時に記録する `contacts.contact_number`（問い合わせ番号）・`contacts.product_id`・`contacts.product_code` は [docs/front/contact.md](../front/contact.md) が正本。本機能では、番号を詳細・CSV・確認ダイアログでの識別に、`product_id` をタブの振り分けと商品詳細への導線に、`product_code` を商品の表示と「削除済み」の判定に使う。

## ユーザーインターフェース仕様（ワイヤーフレーム）

```
一覧（GET /admin/contacts）
お問い合わせ管理                        [絞り込み結果をCSVエクスポート]
+------------------------------------------------------------------+
|  通常のお問い合わせ    商品からのお問い合わせ                        |
|  ━━━━━━━━━━━━                                                    |
+------------------------------------------------------------------+
| ステータス | 受信日 [開始]〜[終了] | キーワード |[クリア] 6件 / 全24件 |
+------------------------------------------------------------------+
| 受信日時         |お名前  |メールアドレス    |本文（抜粋）|状態  |   |
|------------------|--------|------------------|-----------|------|---|
| 2026.08.18 10:30 |山田 太郎|taro@example.com |サイズ選び…|未対応|詳細|
| 2026.08.17 09:12 |佐藤 花子|hanako@example.com|納期につ…|対応済み|詳細|
+------------------------------------------------------------------+
                    [前へ] [1] [2] [次へ]  1ページ50件
```

- タブは「通常のお問い合わせ」「商品からのお問い合わせ」の2つで、件数は出さない。既定は「通常のお問い合わせ」。選択中のタブは下辺に2pxのブランド色の線を引いて示し、背景色は付けない。
- 「商品からのお問い合わせ」タブのみ、メールアドレス列の右に「対象商品」列を表示する。同列は商品名を1段目、商品識別コード（`product_code`）を2段目に出す。
- 本文列は1行に収まる長さで切り、続きがある場合は末尾に `…` を付ける。列見出しは「本文（抜粋）」。
- ページネーションの右に「1ページ50件」を添える。
- キーワードは入力から400ms後に反映する。ステータス・受信日は選択と同時に反映する。タブ・絞り込み条件はクエリストリングに保持し、詳細画面から戻っても状態が維持される。
- 受信日は開始・終了を1つの欄に「〜」で連ねて置き、それぞれ単独でも指定できる（`<input type="date">`）。キーワードの欄はラベルを「キーワード（お名前・メール・対象商品・本文）」とし、プレースホルダーに入力例を示す。
- ステータスはバッジで表示する。配色は `App\Enums\ContactStatus::color()` が正本で、サーバーから `status_tone` として渡す。
- 件数は絞り込みバーの右端に「N件 / 全M件」の形で出す。`M` は表示中のタブの絞り込み前の件数で、タブを切り替えると切り替え先の件数になる。
- 該当0件のときは「該当するお問い合わせがありません」を表示する。
- CSVエクスポートのボタンは「絞り込み結果をCSVエクスポート」とし、表示中のタブと絞り込み条件をそのまま引き継ぐ。

```
詳細（GET /admin/contacts/{contact}）
← お問い合わせ一覧へ
[未対応] 受信日時 2026.08.18 10:30            INQ-2608-0121

+---------------------------+  +---------------------------+
| お問い合わせ内容           |  | 送信者                     |
| サイズ選びについて相談です。|  | 山田 太郎                  |
| （改行を保って表示）        |  | taro@example.com          |
|                           |  | 会員 M-100238              |
| ---                       |  |---------------------------|
| 送信者が入力した内容は管理  |  | 対象商品                   |
| 画面から編集・削除できません|  | チームジャージ 2026 →      |
+---------------------------+  | AP-JRS26                  |
                               +---------------------------+
                               | 対応状況                   |
                               | ステータス                 |
                               | [対応中          ▼]       |
                               | 対応メモ        12 / 2000  |
                               | [                       ]  |
                               | [ 対応状況を更新 ]          |
                               | 最終更新 2026.08.18 11:20  |
                               |  ／ 管理者 佐藤            |
                               | 管理画面からの返信メール送信|
                               | は行いません。              |
                               +---------------------------+
```

- 本文は入力された改行を保って表示する。Markdown 記法として解釈しない（[docs/mail-notification.md](../mail-notification.md) と同じ扱い）。
- 問い合わせ番号を受信日時と同じ行の右端に出す。
- 「お問い合わせ内容」カードの末尾に「送信者が入力した内容は管理画面から編集・削除できません。」を添える。
- 「送信者」の会員IDは、ログイン中に送信された問い合わせのみ表示する。未ログインからの送信は「会員登録なし（未ログインからの送信）」を警告色で表示する。
- 「対象商品」の表示は次の4通りに分かれる。商品名の下には `product_code` がある場合のみ商品識別コードを添える。

| 対象商品の状態 | 表示 |
|---|---|
| 公開中の商品が残っている | 商品名をフロントの商品詳細へのリンクにし、商品識別コードを添える |
| 商品は残っているが非公開 | 商品名と商品識別コードをテキストで表示し、「※ 商品が非公開のため、商品詳細へのリンクはありません」を添える（フロントの商品詳細は非公開商品で404を返すため） |
| 商品が削除済み（`product_code` あり） | 商品名と商品識別コードをテキストで表示し、「※ 商品が削除済みのため、送信時に保存された商品名を表示しています」を添える |
| 手入力（`product_code` が `null` で商品名あり） | 商品名をテキストで表示し、「※ 商品詳細を経由しない手入力のため、商品へのリンクはありません」を添える |
| 対象商品なし | 「なし」と表示する |

- 「対応状況」カードのステータスには「ステータス」のラベルを付ける。対応メモのラベル右に入力文字数を `N / 2000` の形で出し、上限を超えた場合はカウンタを警告色にして「対応メモは2000文字以内で入力してください」を欄の下に出す（送信もしない）。文字数はサーバー側の検証（`mb_strlen` 基準）と揃えて数える。
- 更新は確認モーダル（`ConfirmDialog`）を経て実行し、完了後にトーストを表示する。モーダルは見出しを「対応状況を更新しますか？」、本文を「<問い合わせ番号> のステータスを「<変更後の区分>」に更新し、対応メモを記録します。お客様への通知メールは送信されません。」とする。
- 「最終更新」は一度でも対応状況を更新した場合のみ表示する。対応済み以外の区分では対応日時が無いため、日時の位置に `—` を出す。
- カードの末尾に「管理画面からの返信メール送信は行いません。返信はメールソフトから行い、記録は対応メモに残してください。」を添える。

## インターフェース ＆ データロジック

### データ構造・型定義

```ts
// resources/js/admin/Pages/Contact/Index.tsx
type ContactRow = {
    id: number;
    received_at: string;          // 'YYYY.MM.DD HH:mm'
    name: string;
    email: string;
    body_excerpt: string;         // 本文を60文字で切り、続きがあれば '…' を付けた文字列
    product_name: string | null;  // 商品タブでのみ使用
    product_code: string | null;  // 商品タブでのみ使用。対象商品列の2段目
    status: string;               // ContactStatus の値
    status_label: string;
    status_tone: Tone;            // { fg, bg }
};

type Props = {
    contacts: Paginated<ContactRow>;
    filters: {
        tab: ContactTab;
        status: string;
        q: string | null;
        from: string | null;   // 'YYYY-MM-DD'
        to: string | null;     // 'YYYY-MM-DD'
    };
    totalCount: number;   // 表示中のタブの絞り込み前の件数
    statusOptions: { value: string; label: string }[];
};

// resources/js/admin/Pages/Contact/Show.tsx
type Props = {
    contact: {
        id: number;
        contact_number: string;   // 'INQ-YYMM-NNNN'
        received_at: string;      // 'YYYY.MM.DD HH:mm'
        name: string;
        email: string;
        body: string;
        member_code: string | null;   // 未ログインからの送信は null
        // product_id が残っている場合のみ。is_published はリンクの出し分けに使う
        product: { id: number; name: string; is_published: boolean } | null;
        product_name: string | null;  // 保存された対象商品の文字列
        product_code: string | null;  // 保存された商品識別コード。null なら手入力
        status: string;
        status_label: string;
        status_tone: Tone;
        admin_note: string | null;
        handled_at: string | null;    // 'YYYY.MM.DD HH:mm'
        handled_admin_name: string | null;
    };
    statusOptions: { value: string; label: string }[];
    errors: Record<string, string>;
};
```

タブの値は `resources/js/shared/lib/enums.ts` に `ContactTab`（`general` / `product`）、ステータスの値は同ファイルに `ContactStatus` として定義し、バックエンドの `App\Enums\ContactStatus` と値を揃える。

### 対応ステータスの区分（`App\Enums\ContactStatus`）

| 値 | 表示名 | 用途 |
|---|---|---|
| `unhandled` | 未対応 | 初期値。送信された時点でこの値になる |
| `in_progress` | 対応中 | 担当者が着手済みで、返信・確認が完了していない |
| `handled` | 対応済み | 返信・対応が完了した。対応不要と判断したものもここに含める |

注文のステータスと異なり**遷移の制限は設けない**。どの区分からどの区分へも変更でき、対応済みから未対応へ戻すこともできる。

### 入力値バリデーションルール（`Admin\Contact\UpdateContactRequest`）

| 項目 | ルール |
|---|---|
| `status` | 必須・`App\Enums\ContactStatus` の値のいずれか |
| `admin_note` | 任意・文字列・最大2000 |

### 主要な処理フロー

**一覧（`index()` と `BuildContactFilter`）**

1. タブを適用する。`product` は `product_id` が `null` でない行、`general` は `null` の行。`tab` が未指定・不正な値のときは `general` に丸める。
2. 絞り込みを適用する。
   - `status`: 完全一致（`all` は条件なし）。`App\Enums\ContactStatus` の値の一覧と照合し、外れた値は `all` に丸める
   - `q`: `name` / `email` / `product_name` / `body` のいずれかへの部分一致。`%` と `_` はエスケープする
   - `from` / `to`: `created_at` の期間。開始・終了は片方だけでも指定できる
3. `created_at` の降順（同時刻は `id` の降順）で1ページ50件のページネーションを行い、クエリストリングを引き継ぐ。
4. 表示中のタブの総件数（`totalCount`）は、タブの条件だけを適用し絞り込みを適用せずに数える。

**詳細（`show()`）**

`contacts` の1件と、`user`（会員IDの表示）・`product`（商品詳細への導線）・`handledAdmin`（対応者名）を eager load して返す。

**対応状況の更新（`UpdateContactHandlingService`）**

1. `status` と `admin_note` を保存する。
2. `handled_admin_id` に操作した管理者のIDを設定する。
3. `handled` へ変更した場合は `handled_at` に現在時刻を設定する。`handled` 以外へ変更した場合は `handled_at` を `null` に戻す。同じ区分のまま対応メモだけを更新した場合は `handled_at` を変更しない。
4. 詳細画面へ戻して `対応状況を更新しました。` を表示する。

### 絞り込みクエリの実装方針

- 受信日は境界を明示した範囲比較で行う。開始日は `created_at >= 開始日 00:00:00`、終了日は `created_at < 終了日の翌日 00:00:00` とする。`DATE(created_at)` の形に変換すると `index(status, created_at)` が使えなくなるため、日付関数を条件の左辺に置かない。終了日に指定した日の当日分は結果に含める。
- 日付として解釈できない `from` / `to` は条件なしとして扱う。開始日が終了日より後の場合は条件をそのまま適用し、結果は0件になる。
- `q` の4列への `OR` は1つの括弧でまとめ、タブ・ステータス・受信日の条件と `AND` で結合する。括弧で囲まないと `OR` が他の条件を飲み込む。
- 各列の `LIKE` には `ESCAPE` を明示する。エスケープ文字を既定で持たないDB製品では、指定しないと検索語に含まれる `%` `_` がワイルドカードのまま残る（テスト用のSQLiteが該当する）。
- キーワードは中間一致（`%語%`）のため索引が効かず、`body` は `text` 型で索引の対象にもならない。件数が増えて検索が遅くなった場合は全文検索索引（`FULLTEXT`）の導入を別途検討する。本改修では導入しない。

**CSVエクスポート（`ContactCsvExporter`）**

表示中のタブと絞り込み条件を一覧と同じ規則で適用し、受信日時の降順で全13列を書き出す。書き出しは主キーの降順でキーセット走査する（オフセット走査では、書き出し中に着信した問い合わせで行が重複する）。文字コード・改行コードの扱いは [docs/admin/csv.md](csv.md) が正本。ファイル名は `contacts_YYYYMMDD.csv`。

| 列 | 項目 | 内容 |
|---|---|---|
| A | 問い合わせ番号 | `contact_number` |
| B | 受信日時 | `YYYY-MM-DD HH:MM:SS` |
| C | 種別 | `商品` / `通常`（`product_id` の有無） |
| D | お名前 | `name` |
| E | メールアドレス | `email` |
| F | 会員ID | `users.member_code`。未ログインからの送信・会員削除済みは空 |
| G | 対象商品 | `product_name` |
| H | 商品ID | `product_code`。手入力の問い合わせは空（商品が削除済みでも送信時の値が残る） |
| I | お問い合わせ内容 | `body`。改行を含む |
| J | ステータス | `未対応` / `対応中` / `対応済み` |
| K | 対応メモ | `admin_note` |
| L | 対応者 | `admins.name`。未更新は空 |
| M | 対応日時 | `handled_at` を `YYYY-MM-DD HH:MM:SS`。未対応・対応中は空 |

インポートは提供しない。

## 業務ルール

- 問い合わせの編集・削除は実装しない。送信者が入力した内容（お名前・メールアドレス・対象商品・本文）は管理画面から書き換えられない。変更できるのは対応ステータスと対応メモのみ。この制約は詳細画面にも注記として表示する。
- 管理画面からの返信メール送信は実装しない。返信は管理者のメールソフトから行い、対応の記録は対応メモに残す。
- 対応者は最後に対応状況を更新した管理者で上書きする。誰がいつどの区分へ変更したかの履歴は保持しない。
- 対応メモは更新のたびに送信された値で上書きする。空で送ると消える（画面は常に現在の値を送るため、意図せず消えることはない）。
- タブの振り分けは `product_id` の有無のみで決める。商品詳細を経由せず対象商品を手入力した問い合わせは `product_id` を持たないため「通常のお問い合わせ」に入る。
- 対応メモは管理者専用の記録であり、送信者へは一切表示・送信しない。

## 関連ドキュメント

- [docs/front/contact.md](../front/contact.md) — お問い合わせフォームの正本。`product_id` / `product_name` の記録規則もこちら
- [docs/admin/csv.md](csv.md) — CSVの文字コード・改行コードの正本
- [docs/admin/dashboard.md](dashboard.md) — 未対応件数のサマリー表示
- [docs/admin/common-layout.md](common-layout.md) — `AdminLayout`・`FilterBar`・`DataTable`・`Pagination`・`ConfirmDialog` の正本。サイドメニューへの「お問い合わせ管理」の追加もこちらが正本
- [docs/2_database.md](../2_database.md) — `contacts` の列定義・`ContactStatus`・`Contact` モデル・`ContactFactory` の正本
- [docs/mail-notification.md](../mail-notification.md) — 受付通知・控えメールの正本

## ソースファイル

| 種別 | パス |
|---|---|
| Migration | `database/migrations/2026_08_18_000001_add_product_and_handling_columns_to_contacts_table.php` |
| Migration | `database/migrations/2026_08_21_000001_add_contact_number_and_product_code_to_contacts_table.php` |
| Enum | `app/Enums/ContactStatus.php` |
| Model | `app/Models/Contact.php` |
| Controller | `app/Http/Controllers/Admin/ContactController.php` |
| Controller | `app/Http/Controllers/Admin/ContactCsvController.php` |
| FormRequest | `app/Http/Requests/Admin/Contact/UpdateContactRequest.php` |
| Action | `app/Actions/Admin/Contact/BuildContactFilter.php` |
| Service | `app/Services/Admin/Contact/UpdateContactHandlingService.php` |
| Service | `app/Services/Admin/Csv/ContactCsvExporter.php` |
| Service | `app/Services/Admin/Csv/CsvWriter.php` |
| ルート | `routes/web.php` |
| Page | `resources/js/admin/Pages/Contact/Index.tsx` |
| Page | `resources/js/admin/Pages/Contact/Show.tsx` |
| Component | `resources/js/admin/Components/Contact/StatusBadge.tsx` |
| Component | `resources/js/admin/Components/Contact/TabNav.tsx` |
| Component | `resources/js/admin/Components/Contact/HandlingCard.tsx` |
| 型定義 | `resources/js/shared/lib/enums.ts` |
| Test | `tests/Feature/Admin/Contact/ContactIndexTest.php` |
| Test | `tests/Feature/Admin/Contact/ContactShowTest.php` |
| Test | `tests/Feature/Admin/Contact/ContactUpdateTest.php` |
| Test | `tests/Feature/Admin/Csv/ContactCsvExportTest.php` |

## 受け入れ条件

**一覧**（`tests/Feature/Admin/Contact/ContactIndexTest.php`）

- ログイン済みの管理者が一覧を表示でき、未認証はログイン画面へリダイレクトされる: 同ファイル（`お問い合わせ一覧が表示される`・`未認証ではお問い合わせ一覧を開けない`）
- 既定で「通常のお問い合わせ」タブが選ばれ、`product_id` を持たない問い合わせのみが表示される: 同上（`既定では商品を持たないお問い合わせだけが表示される`）
- 「商品からのお問い合わせ」タブでは `product_id` を持つ問い合わせのみが表示される: 同上（`商品からのお問い合わせタブでは商品を持つものだけが表示される`）
- 対象商品を手入力しただけの問い合わせが「通常のお問い合わせ」タブに入る: 同上（`商品名を手入力しただけの問い合わせは通常のタブに入る`）
- 不正な `tab` の値が「通常のお問い合わせ」に丸められる: 同上（`不正なタブの値は通常のお問い合わせに丸められる`）
- 表示中のタブの総件数が絞り込みの影響を受けず、タブを切り替えると切り替え先の件数になる: 同上（`表示中のタブの総件数は絞り込みの影響を受けない`）
- ステータスで絞り込める・不正なステータス値が `all` に丸められる: 同上（`ステータスで絞り込める`・`不正なステータスの値はすべてに丸められる`）
- お名前・メールアドレス・対象商品・お問い合わせ内容のいずれかへの部分一致で絞り込める: 同上（`キーワードで氏名とメールアドレスと対象商品と本文を検索できる`）
- 絞り込みの検索語に `%` や `_` を含めても部分一致として扱われない: 同上（`検索語のワイルドカードは部分一致として扱われない`）
- キーワードとステータスを同時に指定したとき、キーワードの `OR` 条件がステータスの条件を飲み込まない: 同上（`キーワードの条件はステータスの条件を飲み込まない`）
- 受信日の開始・終了で絞り込め、開始日当日と終了日当日の問い合わせがどちらも結果に含まれる: 同上（`受信日の開始と終了で絞り込め当日分が両端とも含まれる`）
- 受信日を開始のみ・終了のみでも絞り込める: 同上（`受信日は開始だけでも終了だけでも絞り込める`）
- 日付として解釈できない受信日は条件なしとして扱われる: 同上（`日付として解釈できない受信日は条件なしとして扱われる`）
- 開始日が終了日より後のときは0件になる: 同上（`開始日が終了日より後なら結果は空になる`）
- タブと各絞り込みを組み合わせられる: 同上（`タブと絞り込みを組み合わせられる`）
- 一覧が受信日時の降順で並ぶ: 同上（`一覧は受信日時の降順で並ぶ`）
- 1ページに50件を超える場合にページネーションされる: 同上（`五十件を超えるとページネーションされる`）
- 該当0件で空の一覧になる: 同上（`該当がないときは空の一覧になる`）
- 本文が60文字で切られ、続きがある場合のみ `…` が付く: 同上（`本文は六十文字で切られ続きがあるときだけ記号が付く`）
- 商品タブで対象商品の商品識別コードが渡る: 同上（`商品からのお問い合わせタブでは対象商品の商品識別コードが渡る`）
- 文字列以外のクエリが既定値に丸められる: 同上（`文字列以外のクエリは既定値に丸められる`）
- 検索語が `0` でも絞り込みが効く: 同上（`検索語が零でも絞り込みが効く`）
- ページ送りでも絞り込みが維持される: 同上（`ページ送りでも絞り込みが維持される`）
- タブ・絞り込み条件がクエリストリングに保持され、詳細から戻っても維持される: 目視確認
- キーワードのデバウンス（400ms）、ステータス・受信日の即時反映、クリアボタン: 目視確認
- ステータスバッジの配色・タブの下線表示: 目視確認

**詳細**（`tests/Feature/Admin/Contact/ContactShowTest.php`）

- 詳細が表示され、未認証は開けない: 同ファイル（`お問い合わせ詳細が表示される`・`未認証ではお問い合わせ詳細を開けない`）
- 本文・お名前・メールアドレスが送信された内容のまま表示される: 同上（`お問い合わせ詳細が表示される`）
- ログイン中に送信された問い合わせで会員IDが表示され、未ログインからの送信では `null` が渡る: 同上（`ログイン中に送信された問い合わせは会員コードが渡る`・`未ログインから送信された問い合わせは会員コードが空になる`）
- `product_id` を持つ問い合わせで商品IDと商品名が渡る: 同上（`商品からの問い合わせは商品の識別子と商品名が渡る`）
- 商品が削除された問い合わせでも保存された商品名が表示される: 同上（`商品が削除された問い合わせでも保存された商品名が残る`）
- 問い合わせ番号が渡る: 同上（`お問い合わせ詳細が表示される`）
- 商品からの問い合わせで商品識別コードが渡る: 同上（`商品からの問い合わせは商品識別コードが渡る`）
- 商品が削除された問い合わせと手入力の問い合わせを商品識別コードの有無で区別できる: 同上（`商品が削除された問い合わせは商品識別コードが残る`・`手入力の問い合わせは商品識別コードが空になる`）
- 対象商品の注記の書き分け（削除済み／手入力）: 目視確認
- 会員が削除された問い合わせでも送信内容が表示される: 同上（`会員が削除された問い合わせでも送信内容が表示される`）
- 本文の改行が保たれ、Markdown 記法が装飾に変換されない: 目視確認
- 商品詳細へのリンクが機能し、非公開商品ではリンクにならない: 目視確認

**対応状況の更新**（`tests/Feature/Admin/Contact/ContactUpdateTest.php`）

- ステータスと対応メモを更新でき、未認証は更新できない: 同ファイル（`対応ステータスと対応メモを更新できる`・`未認証では対応状況を更新できない`）
- 対応メモの文字数カウンタ・上限超過時のエラー表示: 目視確認
- 更新した管理者が `handled_admin_id` に記録される: 同上（`更新した管理者が記録される`）
- 対応済みへの変更で `handled_at` が操作時刻で記録される: 同上（`対応済みへの変更で対応日時が操作時刻で記録される`）
- 対応メモを送らない更新で既存のメモが消える: 同上（`対応メモを送らない更新では既存のメモが消える`）
- 対応済みから他の区分へ戻すと `handled_at` が `null` に戻る: 同上（`対応済みから他の区分へ戻すと対応日時が消える`）
- 同じ区分のまま対応メモだけを更新しても `handled_at` が変わらない: 同上（`同じ区分のまま対応メモだけを更新しても対応日時は変わらない`）
- どの区分からどの区分へも変更できる（遷移の制限がない）: 同上（`どの区分からどの区分へも変更できる`）
- 不正なステータス値・上限を超える対応メモが拒否される: 同上（`不正な対応ステータスは拒否される`・`上限を超える対応メモは拒否される`）
- 送信者が入力した内容が更新の対象にならない: 同上（`送信者が入力した内容は更新の対象にならない`）
- 確認モーダルとトーストの表示: 目視確認

**CSVエクスポート**（`tests/Feature/Admin/Csv/ContactCsvExportTest.php`）

- 表示中のタブと絞り込み条件が反映され、未認証は書き出せない: 同ファイル（`表示中のタブと絞り込み条件が反映される`・`未認証では書き出せない`）
- 会員・対応者を引くクエリが行数に比例しない: 同上（`会員と対応者を引くクエリが行数に比例しない`）
- 本文の改行がセル内に保たれる: 同上（`本文の改行はセル内に保たれる`）
- 全13列が定義どおりの順序・内容で出力される: 同上（`全ての列が定義どおりの順序で書き出される`）
- 種別列が `product_id` の有無で `商品` / `通常` に出し分けられる: 同上（`種別は対象商品の有無で出し分けられる`）
- 未ログインからの送信で会員ID列が空になる: 同上（`未ログインからの送信では会員の識別子列が空になる`）
- 商品が削除された行でも商品ID列に送信時の商品識別コードが残る: 同上（`商品が削除された行でも商品の識別子列が残る`）
- 手入力の問い合わせは商品ID列が空になる: 同上（`手入力の問い合わせは商品の識別子列が空になる`）
- 未対応・対応中の行で対応日時列が空になる: 同上（`未対応と対応中の行は対応日時列が空になる`）
- 先頭にバイトオーダーマークが付き、改行コードが復帰改行になる: 同上（`先頭にバイトオーダーマークが付き改行は復帰改行になる`）
- 受信日時の降順で書き出される: 同上（`受信日時の降順で書き出される`）
- ダウンロードしたCSVをExcelで開いて文字化けしないこと: 目視確認
