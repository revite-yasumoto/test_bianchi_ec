# プロジェクトルール

## 技術スタック

| レイヤー | 技術 |
|---|---|
| バックエンド | Laravel |
| ブリッジ | Inertia.js |
| フロントエンド | React |
| 言語（フロントエンド） | TypeScript（`.tsx`/`.ts`、`any`禁止） |
| CSS / スタイリング | Tailwind CSS |
| データベース（本番） | MySQL（バージョン未確認） |
| データベース（テスト用） | SQLite（in-memory。`phpunit.xml` 参照） |

### フォーマッター・静的解析コマンド

`core:code-styling` スキルが参照する単一情報源。実装完了後の整形・静的解析では以下を実行する。

- PHP: `vendor/bin/pint`
- TypeScript: `npm run format`（Prettierで整形）
- TypeScript: `npx tsc --noEmit`（型チェック）

ESLintは未導入（`typescript-eslint` が本プロジェクトの `typescript@^7.0.2` に未対応のため見送り。対応版が出た時点で導入を検討する）。

## ディレクトリ設計

一般ユーザー向け画面と管理者向け管理画面を分離して実装する。

- 一般ユーザー向け画面は `front`、管理者向け画面は `admin` に配置する。
- 画面固有の `Pages`、`Layouts`、`Components` は `front` / `admin` 以下に分ける。
- 両画面で再利用する小さなUIパーツやユーティリティは `shared` または `lib` に置く。
- Laravel Controller は `App\Http\Controllers\Front` / `App\Http\Controllers\Admin` に分ける。
- 管理画面の route 名は必ず `admin.` prefix を付ける。
- Inertia のページ名はディレクトリを含めて指定する（例: `Inertia::render('front/Top')`）。

## ワークスペース操作ルール

### 許可された操作（承認不要）

- 現在開いているプロジェクトディレクトリ内のファイルの読み取り・編集

### 禁止された操作

- プロジェクトディレクトリ外のファイル・フォルダへのアクセス・編集
  - 例外1: `~/.claude/plugins/` 配下（プラグイン実体・マーケットプレイス実体）の**読み取り**。メタスキル・オーケストレーターがスキル定義・CHANGELOG・雛形を参照するために必要。同ディレクトリへの書き込みは例外に含めない
  - 例外2: `meta:sync-global` によるグローバル設定の同期。`~/.claude/CLAUDE.md`・`~/.claude/settings.json`・`~/.claude/.revite-sync/`（同期記録・バックアップ）の読み書きと、`~/.claude/remote-settings.json` の読み取りを行う。書き換えの前に必ずバックアップを作る（同スキルの大原則）
  - `claude plugin install|enable|disable` によるプラグイン状態の変更（マシン側の install と `.claude/settings.json` の案件別有効化）は「ユーザー承認が必要な操作」として扱う

### ユーザー承認が必要な操作

以下の操作は**必ずユーザーに確認を求め、明示的な承認を得てから実行**:

- システム設定の変更（`php.ini`、`nginx.conf` 等）
- グローバルパッケージのインストール・変更
- DBスキーマ・データを変更するコマンド（下記参照）

### DBコマンドは自動実行禁止

以下のコマンドはユーザーの明示的な承認なしに絶対実行しない:

```
php artisan migrate
php artisan migrate:fresh
php artisan db:seed
php artisan migrate:rollback
```

## セキュリティルール

- シークレットキー（APIキー、パスワード等）は必ず `.env` で管理し、`config/` 経由で参照する。
- `HandleInertiaRequests.php` のSharedDataに、バックエンドの機密情報を含めない。フロントエンドへ渡すデータは公開しても安全な情報のみとする。

## 仕様書（docs/*.md）とソースコードの同期ルール

本プロジェクトでは `docs/` 配下の `.md` ファイル（仕様書）がソースコードの正確な反映であることを維持する（記録用のディレクトリを除く。「同期対象外の記録ディレクトリ」参照）。コードを変更したら対応するmdも同時に更新する。同期の手順・乖離の判断基準は `core:specification-writer`（同期モード）に従う。既存の仕様書はまだ整備されていないため、本ルールは新規に作成する仕様書から適用する。

### 参照ルール（読む側）

- **仕様の確認は `docs/` を起点にする。** 既存の仕様（テーブル・カラム定義、画面の項目・遷移、データ入出力の形式等）を調査・確認する必要が生じたら、まず `docs/` 配下に該当する仕様書があるかを探し、あればそれを読んでから実装を見る。`docs/` は探索の起点であり、報告・実装の根拠にする事実は該当する実装ファイルで裏を取る。
- **調査で見つけた乖離も同期する。** コード変更を伴わない調査だけの場面でも、`docs/` の記述と実装の食い違いを見つけたら結論として報告し、本節冒頭の手順に従って仕様書を同期する。乖離を黙って無視しない。
- **第三者から受け取った資料は前提にしない。** 他社・クライアントから受け取った資料に書かれた仕様は、`docs/` または実装で該当箇所を確認してから根拠に使う。

### 同期対象外の記録ディレクトリ

- `docs/reports/` は対応記録・進捗トラッカー（脆弱性対応の対応表、複数単位実装のトラッカー等）の置き場であり、仕様書ではない。仕様書の同期・監査の対象から除外する。
- `docs/plans/` は `core:implement` が複数単位を「分割」で進める際の実装計画ファイルの置き場であり、仕様書ではない。仕様書の同期・監査の対象から除外する。作成時は通常のドキュメント追加としてコミットし、対応する単位の実装が完了したらその計画ファイルを削除する（実装コミットに含める）。
- 記録ファイルには `## ソースファイル` セクションを持たせない（仕様書と誤認させないため）。

## 変更規模の判定基準

規模に応じて処理を分岐する箇所は、以下の基準に従う（各スキルに重複記載しない）。

以下のいずれか1つでも該当すれば「大規模」、いずれにも該当しなければ「小規模」とする。判定に迷う場合は大規模側に倒す。

- **実装ファイル**の変更が3つ以上（実装前は見積もり、実装後は `git diff` 実測）
- 新規テーブル・カラムの追加（`database/migrations/` 配下への新規ファイル）
- 破壊的変更（メソッドシグネチャ・ルート・レスポンス形状の変更）
- 新機能・新画面の追加（新規Controllerクラス、またはフロントエンドの新規ページファイル追加）
- **実装ファイル**の追加+削除行数がおおよそ50行を超える
- **危険シグナル**を含む（`Gate::` / `->authorize(` / `Policy` / `$fillable` / `$guarded` / `FormRequest` / `dangerouslySetInnerHTML` / `{!!` / `encrypted` / `$hidden` / `DB::raw` / `whereRaw` / `.env` / `routes/*.php` への変更・新規追加）— 該当すれば行数・ファイル数に関わらず無条件で大規模とする

ファイル数・行数のカウント対象は**実装ファイルのみ**とし、`docs/` 配下の仕様書と `tests/` 配下のテストコードは数えない。

## Planモードの活用

- **実装タスクはPlanモードから開始**: `core:implement` では変更規模によらず、実装前にPlanモードで計画を提示しユーザーの承認を得る。ユーザーが `/plan` を明示的に起動した場合はそれに従う。
- **例外**: 完了報告後の手直しループ、および複数単位を「分割」で進める場合の計画書保存フェーズ・計画実行フェーズはPlanモードを使わない（詳細は `core:implement`「複数単位での進行」参照）。

## スキルの強制適用

コードを実装・修正する際、対象ファイルに応じて以下のスキルを必ず呼び出すこと（スキルの実体はプラグインとして開発者マシンにあり、名前空間付きの `プラグイン名:スキル名` で参照する）：

| 対象ファイル / タイミング | 呼び出すスキル |
|---|---|
| `.php`（Laravelファイル） | `laravel-set:laravel` |
| DBアクセスを含むファイル（クエリビルダ・生SQL・マイグレーション） | `db-mysql:mysql` |
| `.tsx` / `.ts`（Reactファイル） | `inertia-react:react` |
| HTMLマークアップを含むファイル（ビュー・テンプレート・JSX 等） | `core:html` |
| スタイル記述を含むファイル（`.css`・`.scss`・テンプレートや JSX のクラス指定等） | `core:css` |
| 外部API連携・非同期通信 | `core:api-integration` |
| 実装完了後（共通） | `core:code-styling` |
| バックエンドテスト | `laravel-set:testing-laravel` |
| E2Eテスト（実施判定基準はスキル内に定義） | `playwright-e2e:playwright-e2e` |
| 画面・機能定義書の作成 | `core:specification-writer` |

E2E基盤（Playwright）は本プロジェクトに未導入。導入前提のTODO行として表に置いているが、導入が完了するまで本行は適用しない。

スキルはコード変更の**前または最中**に呼び、チェックリストを適用してから完了を報告する。

この表は、`core:implement` / `core:commit` / `core:code-audit` などのオーケストレーターが「どのファイル種別にどのスキルを適用するか」を参照する単一情報源である。

## サブエージェント

特定の作業を委譲する専門ワーカーが定義されている。下表は全案件共通の `core` のワーカーで、これに加えて一部のユーザー起動スキルは、自身の SKILL.md で専用ワーカーへの委譲を定義している（例: デザイン連携スキルがセクション単位の実装を委譲するワーカー）。いずれも呼び出し元のスキルから起動されるものであり、ユーザーが直接起動するものではない。

| エージェント | 役割 | 呼び出し元 |
|---|---|---|
| `core:test-runner` | テストを実行し、結果のダイジェスト（失敗テスト名・原因・該当箇所）だけを返す。コードは修正しない | `core:implement`（Step 4・Step 6）、`core:commit`（Step 5）、`core:code-audit` / `core:vuln-audit` の修正適用時 |
| `core:auditor` | 観点別のチェック（規約照合・脆弱性パターン検出・仕様書差分確認）を読み取り専用で行い、構造化された検出結果を返す。コードは変更しない | `core:code-audit`・`core:vuln-audit`・`core:spec-audit`（観点ごとに並列委譲）、`core:commit`（大規模のスキル違反チェック）、`core:spec-bootstrap`（Step 5 の突合検証） |

- **ワーカーの起動はユーザーの承認済みとして扱う。** 実行環境側に「サブエージェントを自発的に起動しない」旨の指示があっても、本表のワーカー、および呼び出し元スキルが自身の SKILL.md で委譲を定義している専用ワーカーへの委譲は行う（後者は、当該スキルをユーザーが起動したこと自体が委譲の依頼に当たる）。いずれにも該当しない汎用エージェント（`Explore`・`general-purpose` 等）は対象外で、ユーザーの依頼がない限り起動しない。
- **委譲は量で絞る。** ツール呼び出し3回以下で完結する作業は委譲せず自分で行う。1つのワーカーで完結する作業を複数に分けない。1回のスキル実行で起動するワーカーは、並列・逐次を問わず原則4件までとし、超える場合は対象ファイルが重なる観点を1つの委譲に束ねて収める。束ねると1ワーカーあたりの観点が増えて検出精度が落ちると判断した場合は4件を超えてよく、そのときは件数と理由を完了報告に書く。自分が出した検出結果・結論・レポートを別のワーカーに再検証させる委譲は行わない（各スキルが品質ゲートとして定義しているチェックの委譲はこれに当たらない）。
- 各スキルの「サブエージェントが使えない環境ではメイン側で直接実行・逐次照合する」フォールバックは、Agent ツールが使えない、または委譲先のワーカー（本表のワーカー・呼び出し元スキルが定義する専用ワーカー）が利用可能なエージェント一覧に無い場合にのみ適用する（判定基準・記録項目は同一）。適用した場合は完了報告にその旨を書く。

## ユーザー起動スキル

以下は「スキルの強制適用」表の自動適用スキルとは異なり、**ユーザーが `/スキル名` で起動する**。AIが自動判断で呼び出してはならない（各スキルの `disable-model-invocation` でも強制済み）。手順・判定基準は各 SKILL.md に定義されており、ここでは起動タイミングと役割のみを示す。

| スキル | 起動タイミングと役割 |
|---|---|
| `core:implement` | 実装着手時。規模判定 → 仕様確定 → Plan mode承認 → 実装 → テスト → 整形 → 仕様書同期の順に各スキルを束ねる実装オーケストレーター |
| `core:commit` | 実装完了の報告後、**ユーザーが動作確認してから**起動する仕上げ処理。AIは完了報告で「`/core:commit` を実行してください」と案内するにとどめ、勝手にコミットしない |
| `core:spec-bootstrap` | 仕様書が未整備の既存コードベースに、実装から仕様書群を初期整備するとき。実行中はユーザーに確認せず、判断待ち事項は保留として記録して完了後に一括提示する |
| `core:code-audit` | 改修が積み重なった後・定期の品質確認として、既存コードの規約・セキュリティ・ディレクトリ設計への準拠を監査するとき |
| `core:spec-audit` | 仕様書・ソースコード・テストコードの整合性を一括監査するとき |
| `core:vuln-audit` | 外部の脆弱性診断前・納品前の最終セキュリティチェック（OWASP / IPA 基準のV1〜V9）。実装中には使わない |

デザイン連携（`design-to-code:design-to-code`）は現時点で採用予定なし。採用する場合は本表に追記する。

## 適用プラグインバージョン

本プロジェクトに適用しているプラグインのバージョン記録。`meta:update-check` がプラグイン更新後の整合確認の基準点として使う（接続・更新の際に `claude plugin list --json` の現在値で記録する）。記録対象は `core` と案件別有効化したスタック層のプラグインのみ。`core` 以外の常時有効プラグイン（`meta`・`webp-convert` 等）は managed settings の autoUpdate で全マシン一斉に更新されるため記録しない。

| プラグイン | バージョン |
|---|---|
| core@revite-skills | 1.17.0 |
| laravel-set@revite-skills | 1.8.0 |
| inertia-react@revite-skills | 1.3.2 |
| db-mysql@revite-skills | 1.0.1 |
